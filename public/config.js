// Runtime configuration, overwritten when the container starts (see docker/ entrypoint).
// Empty here so dev servers and static hosts fall through to the build-time VITE_* values.
window.__EVENT_CONFIG__ = {}
